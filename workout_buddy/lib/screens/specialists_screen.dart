// map_screen.dart
import 'package:flutter/material.dart';

class SpecialistsScreen extends StatelessWidget {
  const SpecialistsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Map View'));
  }
}

// Repeat for community_screen.dart, marketplace_screen.dart, specialists_screen.dart
// with appropriate class names
